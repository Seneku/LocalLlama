{
  "name": "llama-tuner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run scripts/dev.ts",
    "build": "tsc --noEmit && vite build",
    "build:standalone": "bun run build && bun run scripts/inline-frontend.ts",
    "start": "bun server/index.ts",
    "start:standalone": "bun run build:standalone && bun run server/standalone.ts",
    "package": "bun run build:standalone && bun build --compile --minify --outfile dist/LlamaTuner.exe server/standalone.ts",
    "restart": "scripts/restart-llamatuner.cmd",
    "test": "bun test"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.0.4",
    "vite": "^7.1.10",
    "typescript": "^5.9.3",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "lucide-react": "^0.468.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.22",
    "@types/node": "^24.6.0",
    "@types/react": "^19.1.12",
    "@types/react-dom": "^19.1.9"
  }
}
